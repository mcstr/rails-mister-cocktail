<%= simple_form_for [ @cocktail, @review ] do |f| %>
  <%= f.input :rating, collection: (1..5).to_a, label: "Rate the Cocktail" %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
